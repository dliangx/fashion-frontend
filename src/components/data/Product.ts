type ProductInfo = {
    id: number,
    name: string,
    category: string,
    pic: string,
    brand: string,
    rating: number,
    price: number;
}

type CollectionInfo = {
    id: number,
    pic: string,
    name: string,
}